# IT基礎知識クイズ 仕様書

| | |
|---|---|
| ステータス | **プロトタイプ期**（仕様書は暫定・TBD あり） |
| 最終更新 | 2026-08-24 |
| 対象リポジトリ | `engineer-quiz`（単一 HTML / GitHub Pages 公開） |

---

## 0. この文書について

- **生きた文書**。開発が進むにつれてアップデートされる。
- 現時点は「プロトタイプ先行・仕様書は後追い」の段階。**確定していない箇所は `TBD` と明記**し、誰がいつ埋めるかを [5. 未決事項](#5-未決事項tbd-一覧) に集約する。
- 実装の詳細（作問規約・カテゴリ命名規則など）は `.claude/skills/quiz/SKILL.md` に置き、この文書からは参照する（二重管理を避ける）。

---

## 1. 目的

**IT基礎知識（サーバー・フロントエンド・Git操作）が開発全体でどう関わるかを、体系的に理解できる状態を目指す。**

ゴールの解釈:

- 用語を単体で暗記した状態ではなく、**分野をまたぐ「つながり」を自分の言葉で説明できる**状態をゴールとする。
  - 例: 「画面が真っ白」からブラウザ／通信／サーバー／DB のどこで止まったかを切り分けられる。
  - 例: 「なぜ金額計算をサーバー側に置くのか」をフロントの性質から説明できる。
- したがって出題も、用語 → 仕組み → つながり の順に段階を持たせる（[4.3](#43-分野とレベルの体系) 参照）。

想定利用者: プロトタイプ担当本人および近いスキルセットの新メンバー。**TBD**（新メンバー向けに配る前提にするかで、前提知識の下限と解説の丁寧さが変わる）

---

## 2. 進め方

### 2.1 方針

最初は仕様書を作らず、**簡易的な共通のプロトタイプから互いにブラッシュアップして、マージしたものを仕様書として提案する形式**とする。

- 先に仕様を固めない理由: 「楽しい出題方式」など、実物を触らないと評価できない要素が中心にあるため。
- したがってこの文書は、**プロトタイプの現状追認**から始まり、議論の結論を吸収して育つ。

### 2.2 スケジュール

| 期間 | 内容 | 担当 |
|---|---|---|
| 〜2026年8月末 | プロトタイプを作る | プロトタイプ担当 |
| 2026年9月〜 | プロトタイプをブラッシュアップする | プロトタイプ担当 ＋ レビュー担当 |
| 2026年9月中 | ブラッシュアップ結果をいい感じに合体して、仕様書に昇華する | プロトタイプ担当 ＋ レビュー担当 |

### 2.3 この文書の更新ルール

| 段階 | この文書の扱い |
|---|---|
| プロトタイプ期（〜8月末） | 実装が先。この文書は**現状を書き取る**役割にとどめ、決めていないことは TBD で残す |
| ブラッシュアップ期（9月〜） | 議論の**結論**を反映する。TBD を1件ずつ潰し、潰した日付を [6. 更新履歴](#6-更新履歴) に残す |
| 昇華（9月中） | TBD が残っていないことを確認し、正式な仕様書とする |

---

## 3. 現状の仕様方針

### 3.1 IT基礎知識を収集し、出題内容の基盤とする

YouTube などから IT基礎知識を収集し、出題内容の基盤とする。

現時点で参照した情報源:

| 種別 | 内容 |
|---|---|
| YouTube | 【Git入門】Git + Github使い方入門講座（Gitの仕組みと使い方） |
| YouTube | IT用語66選！エンジニアリングの基本をこの動画1本で！【非エンジニア必見】 |
| YouTube | 【超初心者向け】はじめてのWebサービス、アプリの作り方をわかりやすく解説 |
| YouTube | しまぶーのIT大学（チャンネル / IT基礎全般） |
| Zenn | モダンWeb開発の全体像（4層モデル：フロント／バックエンド／データ・AI／インフラ、CI/CD、IaC、監視） |
| Zenn | モダンなWeb開発の全体像（CSS設計、TypeScript、SEO、セキュリティ、テスト） |
| Qiita | Gitの基本から応用（3層構造、git-flow / GitHub Flow、reset / revert / rebase / cherry-pick / stash、トラブルシュート） |

収集の運用（体系のどこが埋まっていて、どこが空いているかの判定方法）: **TBD**

### 3.2 収集した基礎知識と現行PJを絡めて作問する

「収集した基礎知識」を「**現行PJ**」といい感じに絡めて作問する。

原則（方針レベルで確定）:

- **基礎知識だけで終わらせない。** 一般論を、現行PJで実際に起きた状況に置き換えて問う。
- **PJ固有の判断だけで終わらせない。** その判断の土台にある基礎知識へ接続し、なぜそう判断できるのかを説明できる形にする。
- 基礎知識側と現行PJ側は**別々のカテゴリに分けず、1問の中で結ぶ**ことを狙う（分けると「知識」と「現場」が接続されないままになるため）。

具体の紐付け設計（どの基礎知識を、PJのどの場面と結ぶか）: **TBD**

現行PJの前提情報（サービス概要 / 技術スタック / 関わる工程 / 実際に起きた事象の棚卸し）: **TBD**

再利用できる素材:

- 旧・案件特有 173問が git 履歴（コミット `88d57b6`）に残っている。内訳は 設計判断 65 / スコープ設計 41 / デバッグ・環境 24 / セキュリティ 10 / テスト・スコープ 9 / Linter・CI 7 / その他 17。
- これを現行PJ由来の実例ストックとして流用するかどうか: **TBD**

### 3.3 なんだか楽しい出題方式にする

**具体的手法は未定。**

| 項目 | 内容 |
|---|---|
| 決める時期 | 2026年9月（ブラッシュアップ期） |
| 決める人 | プロトタイプ担当 ＋ レビュー担当 |
| 判断基準 | **TBD** |
| 候補 | **TBD**（この文書では予断を置かず、ブラッシュアップの場で出す） |

---

## 4. 現状のプロトタイプ（2026-08-24 時点の実装）

「共通のプロトタイプ」として現在動いているものの仕様。ここはブラッシュアップの対象であり、確定仕様ではない。

### 4.1 構成

- **単一 HTML ファイル**（`index.html`）で完結。ビルド工程・外部ライブラリ・サーバーなし。
- ホスティングは GitHub Pages（`main` ブランチ直下を公開）。
- HTML のキャッシュは meta タグで抑止（更新が即反映されるように）。
- フォントのみ Google Fonts（Noto Sans JP）を参照。
- 記録はすべてブラウザの localStorage に保存。アカウント・サーバー側の記録は持たない。

### 4.2 出題データ

`index.html` 内の 2つのデータで完結する。

**`QUESTIONS`（現在 150問）** — 1問 = 1オブジェクト

| キー | 内容 |
|---|---|
| `lv` | レベル 1〜3（[4.3](#43-分野とレベルの体系)） |
| `cat` | `<分野> / <小分類>` 形式のカテゴリ。分野の判定にも使われる |
| `situation` | 状況説明。「(1)…、(2)…」形式の候補列挙は表示時に箇条書きカードへ自動整形される |
| `q` | 設問文 |
| `options` | 選択肢（2〜4個、4択が基本）。**出題時に毎回シャッフルされる** |
| `answer` | 正解の添字 |
| `explain` | 解説。なぜ正しいか＋なぜ他が誤りかを含める |
| `track` | （任意）分野の明示指定。通常は `cat` から自動判定されるため不要 |

**`GLOSSARY`（現在 280語）** — `"用語": "平易な説明"` の対応表

- 問題文・選択肢・解説の中に出てくる登録語を自動検出し、下線付きで表示する。
- カーソル / タップで説明をポップアップ。長い語を優先してマッチし、同一テキスト内では各語1回だけハイライトする。英数字の語は単語境界を判定して誤検出を防ぐ。

### 4.3 分野とレベルの体系

**分野（入り口の4択）** — `cat` の ` / ` より前の語で自動判定される

| 分野 | 問題数 | 小分類 |
|---|---|---|
| 🗺 開発の全体像 | 30 | リクエストの流れ / 登場人物と役割 / 開発フローとチーム / つながりの切り分け |
| 🖥 サーバーサイド | 40 | HTTPとAPI / データベース / 実行環境とインフラ / ネットワークとDNS / 認証とセキュリティ |
| 🎨 フロントエンド | 40 | ブラウザの仕組み / HTML と CSS / JavaScript と型 / フレームワークと描画 / 通信とパフォーマンス |
| 🌿 Git とチーム開発 | 40 | 仕組みと3つの領域 / ブランチ運用 / リモート操作 / 履歴の修正 / チーム開発とPR |

- 「開発の全体像」は**分野をまたぐ横断問題の置き場**を兼ねる（目的の「つながり」を担う中心）。
- 判定できないプレフィックスの問題は「開発の全体像」に落ちる。

**レベル（3段）** — 目的の「用語 → 仕組み → つながり」に対応

| レベル | 意味 | 問題数 |
|---|---|---|
| 初級 | 用語と役割。「それは何か」「何のためにあるか」を言える | 35 |
| 中級 | 仕組みと操作。「どう動くか」「実際にどうするか」がわかる | 68 |
| 上級 | つながりと切り分け。分野をまたぐ因果を辿り、原因を絞り込める | 47 |

### 4.4 出題と復習の仕組み

**画面の流れ**: 分野を選ぶ → レベルを選ぶ → カテゴリと出題モードを選ぶ → 出題 → 結果

**出題モード**

| モード | 内容 |
|---|---|
| 復習 | 期限が到来した問題だけを出す（既定） |
| 全問 | 選択したカテゴリの全問をランダムに出す |

**間隔反復（Leitner の箱）** — 数値スコアを持たず、理解度ラベルと最終学習日だけで判定する最小構成

| 箱 | ラベル | 次に出るまで |
|---|---|---|
| box1 | △ まだ自信なし | 約1日 |
| box2 | ○ なんとなく | 約7日 |
| box3 | ◎ 説明できる | 約30日 |

箱の動き: 自力で正解 → 1つ上げる / ヒントを使って正解 → 据え置き / 間違い → box1 に戻す

**「分かったつもり」対策**

- 回答前に**自信の有無を先に選ばせる**（選ぶまで選択肢を表示しない）。
- 回答後、自信 × 正誤の4象限でメッセージを出す。
- **自信あり × 不正解**は「思い込み」として記録し、正解した後も消さず繰り返し出題する（結果画面でも件数を強調）。

**段階ヒント（最大3段）** — 各問にヒント文を持たせず、選択肢から動的に生成する

1. カテゴリと着目点を示す（選択肢は減らさない）
2. 明らかに筋の悪い選択肢を消して3択程度に絞る
3. 2択まで絞る（最後の救済）。正解は決して隠さない

**その他**

- 進捗バー、スコア、自己ベスト記録。
- 問題単位の削除（この端末では以後出題されない。復元も可能）。
- 選択肢は毎回シャッフルし、ラベル A〜D は表示位置に振る（正解位置の偏りを表示側で吸収）。
- 正誤は色だけでなく ✓ / ✕ の記号でも示す。`prefers-reduced-motion` を尊重。フォーカス移動などのキーボード操作に配慮。

### 4.5 端末に保存されるもの

| キー | 内容 |
|---|---|
| `learnquiz_srs_v1` | 問題ごとの理解度の箱・最終学習日・間違い回数・思い込みフラグ |
| `learnquiz_best_v1` | 自己ベスト記録 |
| `learnquiz_deleted_v1` | 削除した問題 |

複数端末・複数人での記録共有: **TBD**（現状は端末ローカルのみ）

### 4.6 作問・用語追加の運用

- `/quiz` スキル（`.claude/skills/quiz/SKILL.md`）で、スクショや状況説明から作問して push するまでを一連で行う。
- 作問規約（カテゴリ命名、レベルの付け方、誤答の作り方、選択肢の文字数バランス、用語追加）は SKILL.md 側に集約。
- 追加後は `node .claude/skills/quiz/scripts/validate.js` による機械チェックを必須とする（構文・重複・択数・answer 範囲・空選択肢）。

---

## 5. 未決事項（TBD 一覧）

| # | 項目 | 決める人 | 時期 |
|---|---|---|---|
| 1 | 楽しい出題方式の具体的手法と、その判断基準 | プロトタイプ担当 ＋ レビュー担当 | 2026年9月 |
| 2 | 現行PJの前提情報の棚卸し（サービス概要 / 技術スタック / 関わる工程 / 起きた事象） | プロトタイプ担当 | 2026年8月〜9月 |
| 3 | 基礎知識と現行PJの具体的な紐付け設計 | プロトタイプ担当 ＋ レビュー担当 | 2026年9月 |
| 4 | 旧・案件特有173問（`88d57b6`）を実例ストックとして流用するか | プロトタイプ担当 ＋ レビュー担当 | 2026年9月 |
| 5 | 知識体系のカバレッジ判定方法（どこが埋まって、どこが空か） | プロトタイプ担当 | 2026年9月 |
| 6 | 想定利用者を新メンバーまで広げるか（前提知識の下限と解説の粒度が変わる） | プロトタイプ担当 ＋ レビュー担当 | 2026年9月 |
| 7 | 目標問題数と「完成」の定義 | プロトタイプ担当 ＋ レビュー担当 | 2026年9月 |
| 8 | 複数端末・複数人での学習記録の共有方法 | プロトタイプ担当 | 未定 |
| 9 | 分野別・レベル別の到達度の可視化（現状は自己ベストと復習残数のみ） | プロトタイプ担当 | 未定 |

---

## 6. 更新履歴

| 日付 | 内容 |
|---|---|
| 2026-08-24 | 初版。目的・進め方・現状の仕様方針（3点）を明文化し、プロトタイプの現状（150問 / 用語280語 / 4分野3レベル）を書き取った |
